{#
/**
 * @file cm_document.html.twig
 * Default theme implementation to present Content Model Document data.
 *
 * This template is used when viewing Content Model Documents.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element. This should contain the `data-uuid` attribute needed for
 *   the loading to work.
 * - uuid: The UUID of the CMDocument.
 * - cm_document_revisions_table: The CM Document entity.
 *
 * @see template_preprocess_cm_document()
 *
 * @ingroup themeable
 */
#}
<div {{ attributes }}>
  {% if content -%}
    <span>{{- content -}}</span>
  {%- endif %}
</div>
